{
  "name": "ai-subak-game-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build && node scripts/prepare-sites-dist.mjs",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "matter-js": "^0.20.0",
    "vite": "^7.0.6"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
